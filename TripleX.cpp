// importing iostream in C++. There's no direct equivalent in Java, but it's necessary for input and output in C++.
#include <iostream>

// Creating the main() function, and int is its return type.
int main()
{
    // std::cout is used for output. In Java, the equivalent would be System.out.println("Hello, World!");
    std::cout << "You are a secret agent breaking into a secure server room.";
    std::cout << " Enter the correct code to continue....";
    return 0;
}
